namespace Xen23.Core
{
    public enum ItemActionType
    {
        None,
        OpenPanel,
        TriggerEvent,
        LoadScene,
        ExitGame,
        PlayAnimation,
        TriggerCallback
    }
}